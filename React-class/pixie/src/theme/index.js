import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3a8bcd",
    },
  },
  shape: {
    borderRadius: 0,
  },
});

export default theme;
